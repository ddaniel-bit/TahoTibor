<Window x:Class="TahoTibor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TahoTibor"
        mc:Ignorable="d"
        Title="Chat App" Height="450" Width="800"
        xmlns:sys="clr-namespace:System.Windows.Data;assembly=PresentationFramework">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Chat messages area -->
        <ListView x:Name="ChatListView" Grid.Row="0" Margin="10" 
                  BorderThickness="1" BorderBrush="#DDDDDD" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ItemsSource="{Binding Messages}"
                  HorizontalContentAlignment="Stretch">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="5" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" 
        Background="#E1E1E1" 
        CornerRadius="10" 
        Padding="10" 
        Margin="0,0,40,0"
        MaxWidth="400"
        HorizontalAlignment="Left"
        Visibility="{Binding IsFromMe, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverse}">
                            <StackPanel>
                                <TextBlock Text="{Binding SenderName}" FontWeight="Bold" Margin="0,0,0,2"/>
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Timestamp}" FontSize="10" HorizontalAlignment="Right" Margin="0,4,0,0" Foreground="#777777"/>
                            </StackPanel>
                        </Border>

                        <!-- My message (right-aligned) -->
                        <Border Grid.Column="1" 
        Background="#0078D7" 
        CornerRadius="10" 
        Padding="10" 
        Margin="40,0,0,0"
        MaxWidth="400"
        HorizontalAlignment="Right"
        Visibility="{Binding IsFromMe, Converter={StaticResource BoolToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Foreground="White"/>
                                <TextBlock Text="{Binding Timestamp}" FontSize="10" HorizontalAlignment="Right" Margin="0,4,0,0" Foreground="#DDDDDD"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Message input area -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="MessageTextBox" Grid.Column="0" Padding="8" Margin="0,0,5,0"
                     KeyDown="MessageTextBox_KeyDown"/>

            <Button x:Name="SendButton" Grid.Column="1" Content="Send" Padding="15,8" 
                    Click="SendButton_Click"/>
        </Grid>
    </Grid>
</Window>